import styled from "styled-components"
import css from "theme/variables"

export default styled.span`
  font-family: ${css.font.openSans};
  font-weight: ${css.fontWeight.semibold};
  font-size: 14px;
  line-height: 18px;
  color: ${css.color.valencia};
`
